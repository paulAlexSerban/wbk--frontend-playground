{
  "name": "front-end-component-library",
  "version": "1.1.0",
  "scripts": {
    "/":"DOCKER",
    "start:compose": "docker-compose --env-file .env up -d --build",
    "stop:compose": "docker-compose down --volumes --rmi all",
    "//":"LIBRARY",
    "install:library":"npm install --prefix library",
    "compile:library":"npm run compile --prefix library",
    "///":"ASSETS",
    "install:assets": "npm install --prefix assets",
    "build:assets": "npm run build --prefix assets"
  },
  "license": "ISC"
}
