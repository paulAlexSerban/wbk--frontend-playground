<body class="typography-page">
    <main class="page-shell">
        <header class="page-header">
            <p class="eyebrow">Component</p>
            <h1>Typography</h1>
            <p class="page-copy">Typography patterns for consistent and accessible text styling.</p>
        </header>

        <section class="demo-grid">
            {{> _paragraph}}
        </section>

        <section class="demo-grid">
            {{> _heading}}
        </section>

        <section class="demo-grid">
            {{> _time-text}}
        </section>
    </main>
</body>