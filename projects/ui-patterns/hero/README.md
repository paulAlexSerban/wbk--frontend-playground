# Hero

Hero layout patterns including split-screen interaction.

## Improvements and expansions to consider:
- add responsive stress cases for long headlines and copy
- add image strategy notes (crop/focal-point/srcset)
- verify CTA focus order and contrast requirements
- add support for video backgrounds and autoplay considerations
- add support for accessible image descriptions and alt text
- add support for different headline levels and semantic structure
- add support for different layout variations (e.g. centered, left-aligned, right-aligned)
- add support for different content types (e.g. product features, event promotion)
- add support for different interaction patterns (e.g. scroll-triggered animations, hover effects)