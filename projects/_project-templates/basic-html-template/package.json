{
  "name": "@wbk-frontend-forge/_project-templates__basic-html-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ && cp manifest.json dist/"
  }
}